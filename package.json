{
  "name": "monorepo-template",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "lerna run --stream --sort build"
  }, 
  "workspaces": [
    "packages/*"
  ], 
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
